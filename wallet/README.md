# Oracle Wallet Directory

This directory is intended to store Oracle Autonomous Database / secure connection wallet files (such as `cwallet.sso`, `ewallet.p12`, `sqlnet.ora`, `tnsnames.ora`, etc.).

Please place your actual Oracle wallet files in this directory. They are ignored by Git for security reasons.